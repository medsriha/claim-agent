# Copy to .env for local development. Never commit .env.

# Process
ENVIRONMENT=local
LOG_LEVEL=INFO
JSON_LOGS=false

# ShipBob mock API
SHIPBOB_BASE_URL=http://localhost:8080
SHIPBOB_TIMEOUT_SECONDS=10

# Model access
ANTHROPIC_API_KEY=
MODEL=claude-opus-5

# Claim policy (see src/claim_agent/policy.py — provisional values need sign-off)
POLICY_REIMBURSEMENT_CAP_USD=100.00
POLICY_MAX_CLAIM_AGE_DAYS=60
POLICY_HIGH_VALUE_ORDER_USD=500.00
POLICY_MIN_ASSESSMENT_CONFIDENCE=0.7
POLICY_MAX_AGENT_STEPS=12
POLICY_MAX_TOOL_RETRIES=2
