"""Pure domain models and rules — claim lines, evidence, assessments, reports.

No I/O, no framework imports, no LLM calls. Everything here is directly unit
testable; that is the point of the package existing (testability).
"""
